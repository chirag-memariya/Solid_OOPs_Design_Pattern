apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  type: NodePort  # Expose as NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 32000  # Specify a nodePort, adjust as needed

